export const DEFAULT_MATRIX_SIZE = 3; // Kích thước ma trận mặc định

export const CELL_PADDING = 8;
export const TEXTFIELD_MIN_WIDTH = 70;
export const TEXTFIELD_MIN_HEIGHT = 45;

export const ITEM_WIDTH = TEXTFIELD_MIN_WIDTH + CELL_PADDING * 2;
export const ITEM_HEIGHT = TEXTFIELD_MIN_HEIGHT + CELL_PADDING * 2;

export const HEADER_SIZE = 50;
export const SCROLLBAR_SIZE = 17;